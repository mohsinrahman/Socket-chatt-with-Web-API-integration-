import { IGif } from '@giphy/js-types';
declare type Props = {
    gif: IGif;
    className?: string;
};
declare const Attribution: {
    ({ gif, className }: Props): JSX.Element | null;
    className: string;
};
export default Attribution;
